node_modules
dist
*.local
.DS_Store

# Lokale Arbeitsordner im Wurzelverzeichnis (Quellmaterial; optimierte
# Fassungen liegen in /public). Mit führendem Slash verankert, damit
# src/modules/materialien und public/materialien NICHT ignoriert werden.
/Materialien/
/Bilder-Eingang/
